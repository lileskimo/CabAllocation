# Server Configuration
PORT=4000

# Database Connection (Example for PostgreSQL)
DB_USER=postgres
DB_HOST=localhost
DB_DATABASE=cab_allocation_db
DB_PASSWORD=your_db_password
DB_PORT=5432

# JWT Secret for Authentication
JWT_SECRET=a_very_strong_and_secret_key_for_jwt

